server:
  port: 8030

eureka:
  client:
    serviceUrl:
      defaultZone: http://${services.registry-service.host}:${services.registry-service.port}/eureka/

management:
  tracing:
    sampling:
      probability: 1.0