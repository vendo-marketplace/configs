server:
  port: ${server.ports.notification-service}

eureka:
  client:
    serviceUrl:
      defaultZone: http://${server.host}:${server.ports.registry-service}/eureka/

management:
  tracing:
    sampling:
      probability: 1.0

mailer:
  message:
    from: "vendo.marketplace.real@gmail.com"