server:
  port: 8060
  host: localhost

eureka:
  client:
    serviceUrl:
      defaultZone: http://${server.host}:${server.ports.registry-service}/eureka/

management:
  tracing:
    sampling:
      probability: 1.0